# Basis Data

> Praktikum basis data — Django + LibreOffice Base.

## Projects
- `projects/studenttracker/` — Django app (`manage.py`, `tracker/models.py`, `tracker/views.py`) + `New Database1.odb` (1796 bytes).

## Cara
```bash
cd projects/studenttracker
pip install django
python manage.py runserver # http://127.0.0.1:8000
```
