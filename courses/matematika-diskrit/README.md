# Matematika Diskrit

> Final project risiko stroke — Flask + ML.

## Projects
- `projects/risiko-stroke/` — `app.py`, `models.py`, `utils.py`, `pdf_generator.py`, `templates/` (base.html, index.html, result.html, admin_panel.html), `static/style.css`, `requirements.txt`, `documentation.md`.

## Cara
```bash
cd projects/risiko-stroke
pip install -r requirements.txt
python app.py # http://127.0.0.1:5000
```
